[package]
name = "importer"
version = "0.1.0"
edition = "2024"

[features]
xlsx = ["dep:calamine"]

[dependencies]
domain = { workspace = true }
serde_json = { workspace = true }
csv = { workspace = true }
calamine = { version = "0.35", optional = true }
